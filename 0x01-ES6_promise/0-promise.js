#!/usr/bin/node
export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // The function currently does not do anything in particular and does not resolve nor reject
  });
}
